{
  "name": "outer-wilds-themed-new-tab",
  "version": "1.2.0",
  "description": "Outer Wilds 风格 3D 篝火新标签页 Chrome 扩展，内置番茄钟、TODO、快捷导航与稍后再看。",
  "main": "app.js",
  "scripts": {
    "build": "rollup -c",
    "check": "node --check app.js && node --check audio.js && node --check background.js && node --check confirm-dialog.js && node --check feature-settings.js && node --check newtab-main.js && node --check pomodoro-ui.js && node --check pomodoro.js && node --check quick-links.js && node --check read-later.js && node --check storage-adapter.js && node --check todo.js && npm run build",
    "start": "node scripts/serve-static.mjs 6287",
    "pack:extension": "npm run check && rm -rf release && mkdir -p release && zip -r release/outer-wilds-new-tab.zip manifest.json newtab.html index.html styles.css newtab-main.js feature-settings.js todo.js quick-links.js read-later.js pomodoro.js pomodoro-ui.js confirm-dialog.js audio.js storage-adapter.js background.js dist assets icons",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "chrome-extension",
    "new-tab",
    "threejs",
    "pomodoro",
    "outer-wilds"
  ],
  "author": "KateLiao",
  "license": "ISC",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/KateLiao/Outer-Wilds-themed-new-tab.git"
  },
  "bugs": {
    "url": "https://github.com/KateLiao/Outer-Wilds-themed-new-tab/issues"
  },
  "homepage": "https://kateliao.github.io/Outer-Wilds-themed-new-tab/",
  "type": "module",
  "dependencies": {
    "three": "^0.165.0"
  },
  "devDependencies": {
    "@rollup/plugin-node-resolve": "^16.0.3",
    "rollup": "^4.62.2"
  }
}
